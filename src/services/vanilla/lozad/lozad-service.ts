import { transient, customElement, inject, bindable, bindingMode, observable, DOM, singleton, noView } from 'aurelia-framework';

import * as lozad from 'lozad';
@singleton()
export class LozadService {
  constructor() {
    //lozad()
  }

}
