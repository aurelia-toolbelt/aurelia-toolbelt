{
  "private": true,
  "scripts": {
    "build": "lerna exec --bail false --concurrency 1  -- yarn run build",
    "watch:core": "yarn workspace @aurelia-toolbelt/core run watch",
    "watch:bootstrap": "yarn workspace @aurelia-toolbelt/bootstrap run watch",
    "watch:admin-tmpl": "yarn workspace admin-template run watch",
    "test:unit": "lerna exec --bail false --concurrency 1  -- yarn run test:unit",
    "test:e2e": "lerna exec --bail false --concurrency 1  -- yarn run test:e2e"
  },
  "workspaces": [
    "packages/*"
  ]
}
