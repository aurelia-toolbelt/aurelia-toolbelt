{
  "private": true,
  "scripts": {
    "build": "lerna exec --bail false --concurrency 1  -- yarn run build",
    "watch:core": "lerna exec --bail false --concurrency 1 --package core -- yarn run watch",
    "watch:bootstrap": "lerna exec --bail false --concurrency 1 --package bootstrap -- yarn run watch",
    "test:unit": "lerna exec --bail false --concurrency 1  -- yarn run test:unit",
    "test:e2e": "lerna exec --bail false --concurrency 1  -- yarn run test:e2e"
  },

  "workspaces": [
    "packages/*"
  ]
}
